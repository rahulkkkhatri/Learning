name: CI_CD

on:
  push:
    
  pull_request:
    branches:
        - dev
        - qa
        - demo
        - main
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        
        
    #   - name: Setup Node.js
    #     uses: actions/setup-node@v4
    #     with:
    #       node-version: '20'
    #       cache: 'npm'

    #   - name: Install Dependencies and Build
    #     run: |
    #       npm ci
    #       npm run build 

    #   - name: Upload Build Artifact
    #     uses: actions/upload-artifact@v4
    #     with:
    #       name: compiled-app
    #       path: dist/ 
          
  test:
    runs-on: ubuntu-latest
    needs: build 
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

    #   - name: ⬇Download Build Artifact
    #     uses: actions/download-artifact@v4
    #     with:
    #       name: compiled-app
    #       path: dist/ 
          
    #   - name: Setup Node.js
    #     uses: actions/setup-node@v4
    #     with:
    #       node-version: '20'

    #   - name: Install Dev Dependencies for Tests
    #     run: npm install 

    #   - name: Run Tests Against Compiled Code
    #     run: npm test

  deploy:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && contains('main dev qa demo', github.ref_name)
    environment: ${{ github.ref_name == 'main' && 'prod' || github.ref_name }}
    steps:
    #   - name: ⬇Download Build Artifact
    #     uses: actions/download-artifact@v4
    #     with:
    #       name: compiled-app
    #       path: dist/ 
      - name: Deploy
        run: |
            echo "Deployment completed!"